// 暂时的空实现，之后再添加实际功能
export const PhotonService = {
  // 添加必要的方法
  connect: () => console.log('PhotonService connect stub'),
  disconnect: () => console.log('PhotonService disconnect stub'),
  // 其他必要方法...
};

export default PhotonService; 